#ifndef LEVEL3_H
#define LEVEL3_H

#include "game_shared.h"

// to modify the state of the game for level 3
void UpdateLevel3(GameState *state);
// Draw everything for level 3 e.g., maze, player, UI, etc.
void DrawLevel3(const GameState *state);

#endif // LEVEL3_H